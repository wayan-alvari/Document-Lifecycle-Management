# Codex Goal Prompt

Paste the entire block below into Codex as **one message** from the root of this repository. It is one durable Goal command, not a sequence of separate prompts. If `/goal` is not recognized, enable the Codex Goals feature first, then paste this block unchanged.

```text
/goal Build the complete Document Lifecycle Management portfolio application specified in Guide.md, without stopping until every Definition of Done item that can be verified locally passes. Treat Guide.md as the authoritative product, clean-room, security, licensing, architecture, data-reset, testing, and delivery contract. Read Guide.md fully before changing files; inspect the repository, current Git status/history, installed SDKs/tools, and existing files; then create and continuously maintain a concrete milestone plan matching section 16.

Work autonomously through every milestone in order. Build an original clean-room implementation only: never read from, copy, import, or derive implementation details or assets from any employer repository or application. Use only official public dependencies and newly written generic English content. Do not create Docker files. Do not deploy, create hosting resources, or push to a remote.

Use .NET 8/C# 12, the pinned SDK and local dotnet-ef tool, ASP.NET Core MVC/Identity, EF Core with the approved MySQL provider, official npm AdminLTE assets, the structure and versions in Guide.md, central package management, NuGet lock files, and package-lock.json. Keep secrets out of tracked files; use placeholders and document user-secrets/environment-variable setup. If a real MySQL credential is unavailable, finish and verify the normal SQLite-backed automated suite and all code/migrations/documentation, clearly recording the optional MySQL smoke-test command instead of inventing or committing credentials.

Implement the per-browser DemoWorkspace isolation and six-hour inactivity reset exactly as specified, including server-side workspace scoping for every domain query/command and cleanup of workspace uploads. Implement all roles, pages, workflows, exports, security controls, English UI, synthetic seed data, tests, README, MIT license with "Copyright (c) 2026 Wayan Alvari", third-party notices, clean-room declaration, and manual non-Docker publish guide. Do not weaken or silently omit a requirement; when an implementation detail is unspecified, choose the smallest secure, maintainable option consistent with a portfolio-sized modular monolith and record the decision in docs.

Git history is a required deliverable. Begin with `git status --short` and preserve any pre-existing user changes. At the end of EACH section-16 milestone (and at any useful smaller coherent split), inspect the diff, run the relevant format/build/test/security checks, stage only that milestone's files, and immediately create the exact conventional commit named in the milestone table. A commit must be cohesive and passing; fix failures before committing. Never postpone all commits until the end, never make one giant implementation commit, and do not squash, amend, rebase, reset, rewrite history, or mix unrelated changes. Never commit secrets, local settings, uploads, node_modules, bin/obj, test results, publish output, or database files. Use the repository's existing Git identity; if it is genuinely missing, report that specific blocker rather than inventing an identity. Never run `git push`.

After every commit, update the goal plan/progress and continue to the next unfinished milestone without waiting for routine confirmation. Ask only when missing authority or information makes a safe implementation impossible; otherwise make documented reasonable assumptions. Before declaring the Goal complete, run the full quality gates from Guide.md, perform a Release publish to an ignored local output directory, review `git diff`, `git status --short`, the complete milestone commit log, dependency/license notices, and the entire tracked history/tree for secrets and prohibited company material. Fix all findings in an additional focused commit. Stop only when the Definition of Done is satisfied, the final working tree is clean, and provide a concise handoff containing demo credentials, local setup/run/test/publish commands, commit summary, and any optional MySQL smoke test that requires the owner's local secret.
```
